{
    "build": {
      "command": "npm install && npm run build",
      "output": "build"
    },
    "start": {
      "command": "npm start"
    },
    "healthcheckPath": "/api/health",
    "cron": "*/5 * * * *"
}
  
